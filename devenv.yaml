# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json

# ALTO repo - module definition
# To switch modes: edit `default = "X"` in devenv.nix, run alto-restart
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

imports:
  - ./devenv.nix
