# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json

# devenv.yaml exists for testing ALTO as a consumer
# For ALTO development, just run `claude` directly - no devenv needed
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

imports:
  - ./devenv.nix  # The ALTO module (not enabled by default)
